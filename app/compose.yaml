---
version: '3.6'
networks:
  frontend:
services:
  web:
    image: 'eemechs/comments-api:latest'
    restart: always
    container_name: 'comments-api'
    ports:
      - '8000:8000'
    networks:
      - frontend